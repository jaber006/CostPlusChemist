import { CartClient } from './cart-client'

export const metadata = {
  title: 'Cart - CostPlus Chemist',
  description: 'Review your cart with full transparent pricing breakdown.',
}

export default function CartPage() {
  return <CartClient />
}
